import { CountFormatPipe } from './count-format.pipe';

describe('CountFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CountFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
